#include "main.h"

/**
 * mul - Multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: the result of the multiplication
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
