#include "main.h"

/**
 * add - Adds two integers
 * @a: first variable in the summation
 * @b: second variable in the summation
 * Return: result of summation
 */
int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
