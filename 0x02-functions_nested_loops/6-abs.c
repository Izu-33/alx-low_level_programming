#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @r: character to be checked
 * Return: r when it is positive. -r otherwise.
 */
int _abs(int r)
{
	if (r >= 0)
	{
		return (r);
	}
	else
	{
		return (-r);
	}
}
